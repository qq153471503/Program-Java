
/**
 * @ClassName：ArrayFunction
 * @Function：对数组的操作功能，例如对数组排序
 * @Author: LiuAnkun
 * @Version: 1.0
 * */
//该类中的方法都是静态的，所以该类无需创建对象 所以将构造函数私有化，强制不创建对象
public class ArraryFunction {

	private ArraryFunction () {
		
	}
	/**
	 * @FunctionName: getArrayMax
	 * @param: arr[]
	 * @Function: 获得数组元素的最大值
	 * @return: 数组中的元素最大值
	 * */
	public static int getArrayMax(int[] arr){

		if(arr.length <= 0)
		{
			System.out.println("Array Error !");
			return -1;
		}

		int max = 0;
		for(int i=0; i<arr.length; i++)
		{
			if(arr[i]>arr[max])
				max = i;
		}
		return arr[max];
	}
}
